import styled from 'styled-components/macro';

export const Wrapper = styled.div`
  max-width: 1200px;
  width: 100%;
  margin: 0 auto;

  .carousel {
    margin-bottom: 20px;
    max-height: 500px;
  }

  .back-btn {
    display: flex;
    align-items: center;
  }
`;

export default Wrapper;
